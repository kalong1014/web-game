[ISAPI_Rewrite]

# 3600 = 1 hour
CacheClockRate 3600

RepeatLimit 32

# Protect httpd.ini and httpd.parse.errors files
# from accessing through HTTP
RewriteRule /index-(.*)\.(htm|html|shtml) /index.php?rewrite=$1 [N,I]
RewriteRule /(mobile)/(.*)\.(htm|html|shtml) /mobile.php\?rewrite=$2 [N,I]